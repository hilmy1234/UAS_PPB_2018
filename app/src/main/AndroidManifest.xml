<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hilmyabcd.uasppb">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splashscreen"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GarutKota" />
        <activity android:name=".Menu" />
        <activity android:name=".Limbangan" />
        <activity android:name=".Samarang" />
        <activity android:name=".Cilawu" />
        <activity android:name=".Bayongbong" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsBall"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".MapsSudirman"
            android:label="@string/title_activity_maps_sudirman" />
        <activity
            android:name=".MapsPesona"
            android:label="@string/title_activity_maps_pesona" />
        <activity
            android:name=".MapsTitan"
            android:label="@string/title_activity_maps_titan" />
        <activity
            android:name=".MapsRCM"
            android:label="@string/title_activity_maps_rcm" />
        <activity
            android:name=".MapsSSS"
            android:label="@string/title_activity_maps_sss" />
        <activity
            android:name=".MapsGGSS"
            android:label="@string/title_activity_maps_ggss" />
        <activity
            android:name=".MapsPanamur"
            android:label="@string/title_activity_maps_panamur" />
        <activity
            android:name=".MapsBifons"
            android:label="@string/title_activity_maps_bifons" />
        <activity
            android:name=".MapsGallery"
            android:label="@string/title_activity_maps_gallery" />
        <activity
            android:name=".MapsHolywood"
            android:label="@string/title_activity_maps_holywood" />
        <activity
            android:name=".MapsToblong"
            android:label="@string/title_activity_maps_toblong"></activity>
    </application>

</manifest>